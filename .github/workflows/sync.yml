
name: Sync Fork

on:
  schedule:
    - cron: '*/30 * * * *' # every 30 minutes
  workflow_dispatch: # on button click

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v1.8
        with:
          token: ${{ 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 }}
          owner: blackmatrix7
          base: master
          head: master
